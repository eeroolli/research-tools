# environment.yml
# Clean environment for research-tools with only actively used dependencies
# Optimized for Intel i7 with UHD 620 GPU

name: research-tools
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11  # Intel Python 3.11 for better optimization
  - opencv  # Intel optimized OpenCV for image preprocessing
  - pyzbar  # Barcode detection
  - pillow  # Image processing
  - pyyaml  # Configuration files
  - pytesseract  # OCR engine (used in scripts/ocr_pdf.py for Tesseract-based OCR)
  - requests  # API calls
  - configparser  # Configuration parsing
  - pdfplumber  # PDF text extraction
  - pymupdf  # PDF manipulation and rotation
  - pytest  # Testing framework
  - mkl  # Intel Math Kernel Library
  - mkl-service
  - intel-openmp  # Intel OpenMP
  - tbb  # Intel Threading Building Blocks
  - watchdog  # File system monitoring for paper processor daemon
  - pathvalidate  # Cross-platform filename sanitization
  - pip
  - pip:
    - pyzotero  # Zotero API integration
    - psutil  # System and process monitoring for CPU throttling
    - beautifulsoup4  # HTML parsing for JSTOR DOI extraction
    # OCR engines (install via pip as needed):
    # - easyocr  # Used in scripts/create_searchable_pdf.py (GPU-accelerated OCR)
    # - paddlepaddle-gpu  # PaddleOCR framework (for Docker container, not local install)
    # - paddleocr  # PaddleOCR (runs in Docker container on p1, not local install)
    # Note: PaddleOCR runs in Docker container (paddleocr-gpu) on p1 with GPU support
    # EasyOCR can be installed locally if needed for create_searchable_pdf.py
